const _baseUrl = '10.0.0.16:8090';

const URL = {
    socketServer: `ws://${_baseUrl}/ws`,
    apiServer: `http://${_baseUrl}/api/config`,
}


const DISPLAY_TYPE = {
    LED: 1,
    CONSOLE: 2,
    WEB: 3
};
